{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "default-next-page",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project that page will be generated into.",
      "alias": "p",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "What is the name of the project for this page?"
    },
    "name": {
      "type": "string",
      "description": "The name of the page.",
      "alias": "n",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the page?"
    },
    "directory": {
      "type": "string",
      "description": "Create the page under this directory (can be nested). Will be created under 'pages/'.",
      "alias": "d"
    }
  },
  "required": ["name", "project"]
}
