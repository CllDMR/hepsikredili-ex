import { FC } from 'react';

/* eslint-disable-next-line */
export interface PageRegisterProps {}

export const Register: FC<PageRegisterProps> = (props) => (
  <div>
    <h1>Welcome to Register!</h1>
  </div>
);

export default Register;
