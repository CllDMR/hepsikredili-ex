import { FC } from 'react';

/* eslint-disable-next-line */
export interface PageLoginProps {}

export const Login: FC<PageLoginProps> = (props) => (
  <div>
    <h1>Welcome to Login!</h1>
  </div>
);

export default Login;
